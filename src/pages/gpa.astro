---
// src/pages/gpa.astro
const siteTitle = "GPA Planner";
---

<html lang="ja">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{siteTitle} | State / Action</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-900 min-h-screen p-4 md:p-12 font-mono selection:bg-blue-100 selection:text-blue-700">
    <div class="max-w-4xl mx-auto space-y-12">
        
        <header class="flex flex-col md:flex-row justify-between items-center py-6 border-b border-slate-200 mb-8">
            <div class="text-center md:text-left">
                <h1 class="text-4xl font-black tracking-tighter text-slate-800 italic">GPA Planner</h1>
                <p class="text-slate-400 font-bold uppercase tracking-widest text-[10px] mt-1">Reverse Engineering Your Grades</p>
            </div>
            <a href="/" class="mt-4 md:mt-0 px-6 py-2 bg-white border border-slate-200 rounded-full text-sm font-bold text-slate-600 hover:bg-slate-50 hover:border-blue-300 hover:text-blue-600 transition-all shadow-sm">
                ← ✍️ 作者のページへ
            </a>
        </header>

        <section class="bg-white rounded-[2.5rem] shadow-xl shadow-slate-200/50 border border-slate-100 p-8 md:p-12">
            <div class="flex items-center gap-6 mb-10">
                <span class="bg-blue-600 text-white w-12 h-12 rounded-2xl flex items-center justify-center font-black text-xl shadow-lg shadow-blue-200 rotate-3">1</span>
                <div>
                    <h2 class="text-3xl font-black text-slate-800 tracking-tight">大学を選択</h2>
                </div>
            </div>
            <div class="relative group">
                <input type="text" id="uniSearch" class="w-full p-6 bg-slate-50 border-2 border-transparent rounded-2xl focus:border-blue-500 focus:bg-white outline-none transition-all font-bold text-lg" placeholder="大学名で検索...">
                <div id="uniList" class="hidden absolute z-20 w-full mt-2 bg-white border border-slate-100 rounded-2xl shadow-2xl max-h-72 overflow-y-auto divide-y divide-slate-50"></div>
            </div>
            <div id="selectedUniBadge" class="mt-6 hidden">
                <div class="inline-flex items-center gap-3 bg-blue-50 text-blue-700 px-6 py-3 rounded-xl border border-blue-100 font-bold animate-in zoom-in duration-300">
                    <span id="targetUniName"></span>
                    <span class="w-1 h-1 bg-blue-300 rounded-full"></span>
                    <span id="targetUniScale" class="text-xs uppercase tracking-tighter"></span>
                </div>
            </div>
        </section>

        <section class="bg-white rounded-[2.5rem] shadow-xl shadow-slate-200/50 border border-slate-100 p-8 md:p-12">
            <div class="flex items-center gap-6 mb-10">
                <span class="bg-blue-600 text-white w-12 h-12 rounded-2xl flex items-center justify-center font-black text-xl shadow-lg shadow-blue-200 -rotate-3">2</span>
                <h2 class="text-3xl font-black text-slate-800 tracking-tight">情報を入力</h2>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <div class="space-y-3">
                    <label class="block text-sm font-black text-slate-500 uppercase ml-2">現在の累計GPA</label>
                    <input type="number" id="currGpa" step="0.01" value="3.4" class="w-full p-5 bg-slate-50 border-2 border-transparent rounded-2xl focus:border-blue-500 focus:bg-white outline-none font-bold transition-all">
                </div>
                <div class="space-y-3">
                    <label class="block text-sm font-black text-slate-500 uppercase ml-2">取得済み単位数</label>
                    <input type="number" id="currCredits" value="80" class="w-full p-5 bg-slate-50 border-2 border-transparent rounded-2xl focus:border-blue-500 focus:bg-white outline-none font-bold transition-all">
                </div>
                <div class="space-y-3">
                    <label class="block text-sm font-black text-slate-500 uppercase ml-2">今後取得予定の単位数</label>
                    <input type="number" id="futureCredits" value="40" class="w-full p-5 bg-slate-50 border-2 border-transparent rounded-2xl focus:border-blue-500 focus:bg-white outline-none font-bold transition-all">
                </div>
                <div class="space-y-3">
                    <label class="block text-sm font-black text-blue-500 uppercase ml-2">目標GPA</label>
                    <input type="number" id="targetGpa" step="0.01" value="3.5" class="w-full p-5 bg-blue-50 border-2 border-blue-200 rounded-2xl focus:border-blue-600 focus:bg-white outline-none font-black text-blue-600 transition-all">
                </div>
            </div>
            <button id="calcBtn" class="w-full mt-10 bg-gradient-to-r from-blue-600 to-indigo-600 text-white font-black py-6 rounded-3xl hover:scale-[1.02] active:scale-[0.98] transition-all shadow-2xl shadow-blue-200 text-xl tracking-tighter italic">
                RUN SIMULATION
            </button>
        </section>

        <div id="resultArea" class="hidden space-y-12 pb-24 animate-in fade-in slide-in-from-bottom-10 duration-1000">
            <header class="flex items-center gap-6">
                <span class="bg-blue-600 text-white w-12 h-12 rounded-2xl flex items-center justify-center font-black text-xl rotate-6">3</span>
                <h2 class="text-3xl font-black text-slate-800 tracking-tighter">シミュレーション結果</h2>
            </header>
            <div class="bg-gradient-to-br from-blue-600 via-indigo-700 to-slate-900 text-white p-12 rounded-[3rem] shadow-2xl shadow-blue-300 relative overflow-hidden group text-center">
                <div class="relative z-10">
                    <p class="text-blue-200 font-bold tracking-[0.3em] uppercase text-xs mb-6">Required Average</p>
                    <h3 id="mainResultText" class="text-3xl md:text-4xl font-black leading-tight mb-8"></h3>
                    <div class="inline-block px-8 py-3 bg-white/10 backdrop-blur-md rounded-full border border-white/20">
                        <span id="avgNeeded" class="text-xl font-mono font-bold"></span>
                    </div>
                </div>
                <div class="absolute -top-24 -right-24 w-80 h-80 bg-blue-400/20 rounded-full blur-3xl group-hover:bg-blue-400/30 transition-all duration-1000"></div>
            </div>
            <div class="grid grid-cols-1 gap-8" id="scenarioContainer"></div>
        </div>
    </div>

    <script>
        const universities = [
            { id: "tokyo", name: "東京大学", gpaSystem: { scale: 4.0, grades: [{ grade: "A+", point: 4.0, label: "優上" }, { grade: "A", point: 3.0, label: "優" }, { grade: "B", point: 2.0, label: "良" }, { grade: "C", point: 1.0, label: "可" }, { grade: "F", point: 0.0, label: "不可" }]}},
            { id: "kyoto", name: "京都大学", gpaSystem: { scale: 4.3, grades: [{ grade: "A+", point: 4.3, label: "A+" }, { grade: "A", point: 4.0, label: "A" }, { grade: "B", point: 3.0, label: "B" }, { grade: "C", point: 2.0, label: "C" }, { grade: "D", point: 1.0, label: "D" }, { grade: "F", point: 0.0, label: "F" }]}},
            { id: "osaka", name: "大阪大学", gpaSystem: { scale: 4.0, grades: [{ grade: "S", point: 4.0, label: "秀" }, { grade: "A", point: 3.0, label: "優" }, { grade: "B", point: 2.0, label: "良" }, { grade: "C", point: 1.0, label: "可" }, { grade: "F", point: 0.0, label: "不可" }]}},
            { id: "waseda", name: "早稲田大学", gpaSystem: { scale: 4.0, grades: [{ grade: "A+", point: 4.0, label: "優上" }, { grade: "A", point: 3.0, label: "優" }, { grade: "B", point: 2.0, label: "良" }, { grade: "C", point: 1.0, label: "可" }, { grade: "F", point: 0.0, label: "不可" }]}},
            { id: "generic_4_0", name: "汎用テンプレート（4.0）", gpaSystem: { scale: 4.0, grades: [{ grade: "S", point: 4.0, label: "秀" }, { grade: "A", point: 3.0, label: "優" }, { grade: "B", point: 2.0, label: "良" }, { grade: "C", point: 1.0, label: "可" }, { grade: "F", point: 0.0, label: "不可" }]}}
        ];

        let selectedUni = universities[4];
        const uniInput = document.getElementById('uniSearch');
        const uniList = document.getElementById('uniList');

        uniInput.addEventListener('focus', () => uniList.classList.remove('hidden'));
        uniInput.addEventListener('input', (e) => {
            const val = e.target.value.toLowerCase();
            const filtered = universities.filter(u => u.name.includes(val));
            renderList(filtered);
        });

        function renderList(list) {
            uniList.innerHTML = list.map(u => `
                <button class="w-full p-5 text-left hover:bg-blue-50 transition-all group flex justify-between items-center" onclick="selectUni('${u.id}')">
                    <span class="font-bold text-slate-700">${u.name}</span>
                    <span class="text-xs font-black text-slate-300 group-hover:text-blue-600 uppercase tracking-widest">${u.gpaSystem.scale} Scale</span>
                </button>
            `).join('');
        }
        renderList(universities);

        window.selectUni = (id) => {
            selectedUni = universities.find(u => u.id === id);
            uniInput.value = selectedUni.name;
            uniList.classList.add('hidden');
            document.getElementById('selectedUniBadge').classList.remove('hidden');
            document.getElementById('targetUniName').innerText = selectedUni.name;
            document.getElementById('targetUniScale').innerText = `${selectedUni.gpaSystem.scale} Scale`;
        };

        // --- バグを根絶する精密ソルバー ---
        const solveUnits = (total, needed, grades) => {
            // 目標を挟む2つの評価を探す
            let high = grades[0], low = grades[grades.length - 1];
            for (let i = 0; i < grades.length - 1; i++) {
                if (grades[i].point >= needed && grades[i+1].point < needed) {
                    high = grades[i];
                    low = grades[i+1];
                    break;
                }
            }
            const x = (total * (needed - low.point)) / (high.point - low.point);
            const top = Math.min(total, Math.ceil(x - 1e-9));
            return { top, mid: total - top, high, low };
        };

        document.getElementById('calcBtn').addEventListener('click', () => {
            const g_curr = parseFloat(document.getElementById('currGpa').value);
            const c_curr = parseFloat(document.getElementById('currCredits').value);
            const c_future = parseFloat(document.getElementById('futureCredits').value);
            const g_target = parseFloat(document.getElementById('targetGpa').value);

            const c_total = c_curr + c_future;
            const g_needed = ((g_target * c_total) - (g_curr * c_curr)) / c_future;

            if (g_needed > selectedUni.gpaSystem.scale) {
                alert("目標が高すぎます！");
                return;
            }

            document.getElementById('resultArea').classList.remove('hidden');
            document.getElementById('avgNeeded').innerText = `必要平均GPA: ${g_needed.toFixed(2)}`;
            
            const res = solveUnits(c_future, g_needed, selectedUni.gpaSystem.grades);
            document.getElementById('mainResultText').innerText = `${res.high.label}を ${res.top}単位、${res.low.label}を ${res.mid}単位で到達`;

            const scenarios = [
                { name: "保守的", desc: "目標にちょうど届くプラン", target: g_needed, icon: "余裕あり" },
                { name: "中間", desc: "少し高みを目指すプラン", target: g_needed + (selectedUni.gpaSystem.scale - g_needed) * 0.4, icon: "かなり厳しい" }
            ];

            document.getElementById('scenarioContainer').innerHTML = scenarios.map(s => {
                const sRes = solveUnits(c_future, s.target, selectedUni.gpaSystem.grades);
                return `
                    <div class="bg-white border-2 border-slate-100 rounded-[2.5rem] p-10 shadow-sm transition-all">
                        <div class="flex justify-between items-start mb-10">
                            <div>
                                <h4 class="text-2xl font-black text-slate-800">${s.name}</h4>
                                <p class="text-slate-400 font-bold text-sm uppercase mt-1">${s.desc}</p>
                            </div>
                            <span class="bg-slate-50 text-slate-600 px-6 py-2 rounded-full text-xs font-black border border-slate-100 uppercase">${s.icon}</span>
                        </div>
                        <div class="grid grid-cols-2 gap-4 mb-10">
                            <div class="bg-slate-50 p-6 rounded-3xl text-center border border-slate-100">
                                <p class="text-[10px] font-black text-slate-400 uppercase mb-2">${sRes.high.label}</p>
                                <p class="text-3xl font-black text-slate-800">${sRes.top} <span class="text-sm font-normal text-slate-400">単位</span></p>
                            </div>
                            <div class="bg-slate-50 p-6 rounded-3xl text-center border border-slate-100">
                                <p class="text-[10px] font-black text-slate-400 uppercase mb-2">${sRes.low.label}</p>
                                <p class="text-3xl font-black text-slate-800">${sRes.mid} <span class="text-sm font-normal text-slate-400">単位</span></p>
                            </div>
                        </div>
                        <div class="flex justify-between items-center pt-8 border-t border-slate-50">
                            <span class="text-slate-400 font-black text-xs uppercase tracking-widest uppercase">Target GPA</span>
                            <span class="text-3xl font-black italic text-slate-800">${s.target.toFixed(2)}</span>
                        </div>
                    </div>
                `;
            }).join('');
            window.scrollTo({ top: document.getElementById('resultArea').offsetTop - 100, behavior: 'smooth' });
        });
    </script>
</body>
</html>